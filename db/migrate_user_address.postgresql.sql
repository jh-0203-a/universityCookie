-- ============================================================
-- 회원 실거주지 주소(address) 컬럼 추가
--  - 회원가입 때 자유 입력으로 받는 실제 거주지 주소(○○동까지)를 저장합니다.
--  - 지역 인증(주민등록증) 심사 때 관리자가 이 주소와 신분증을 대조합니다.
--  - 여러 번 실행해도 안전합니다 (IF NOT EXISTS).
-- ============================================================

ALTER TABLE users ADD COLUMN IF NOT EXISTS address VARCHAR(200);
